# PREMASS Admin Dashboard - Backend Environment Configuration
# Copy this file to .env and update values for your environment

# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================

# Option 1: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/premass-admin

# Option 2: MongoDB with Authentication
# MONGODB_URI=mongodb://username:password@localhost:27017/premass-admin

# Option 3: MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/premass-admin?retryWrites=true&w=majority

# ============================================
# JWT Configuration
# ============================================

# Must be at least 32 characters for production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production-minimum-32-characters

# Refresh token secret - also minimum 32 characters
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production-minimum-32-characters

# Token expiration times
ACCESS_TOKEN_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=7d

# ============================================
# API Configuration
# ============================================
API_BASE_URL=http://localhost:3001/api/v1

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# ============================================
# Email Configuration (Optional)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@premass.com

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=debug
LOG_FILE=logs/app.log

# ============================================
# Security Configuration
# ============================================
BCRYPT_ROUNDS=10
SESSION_SECRET=change-this-to-random-string-32-chars

# ============================================
# Third-party Services (Optional)
# ============================================
SENTRY_DSN=
REDIS_URL=redis://localhost:6379

# Environment
NODE_ENV=development
